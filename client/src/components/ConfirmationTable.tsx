import React from "react";
import {BookItem, LineItem} from "../types";

interface Props {
    lineItems: LineItem[];
}

const ConfirmationTable: React.FC<Props> = ({ lineItems }) => {
    return (
<div></div>
    );
};

export default ConfirmationTable;
